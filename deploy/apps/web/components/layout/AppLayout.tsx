'use client'

// Legacy - no longer used. Dashboard layout is now in app/(dashboard)/layout.tsx
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
