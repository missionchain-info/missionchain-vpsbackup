# Node modules
**/node_modules/
**/.next/

# Env files (contains secrets)
.env
**/.env
**/.env.local
**/.env.production

# Build artifacts
**/dist/
**/build/
**/.turbo/

# Large archives
*.tar.gz
*.zip

# DB files
*.db
*.sqlite

# Logs
logs/
**/*.log

# Docker volumes data
data/postgres/
data/redis/

# OS files
.DS_Store
Thumbs.db

# Large images (will add sophia-avatar separately)
.env.backup.txt

# Archive folder for pre-mainnet cleanup (kept local only)
_archive_pre_mainnet/

# Nested git repo (active dev, has its own remote)
deploy/

# Large media (108+ files, 1.1GB+ — synced separately to VPS)
MFP-ART/

# Reports + crontab dumps
reports/
crontab-backup.txt
pm2-dump.json
